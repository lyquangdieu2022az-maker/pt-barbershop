services:
  - type: web
    name: pt-barbershop-pos
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    branch: main
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: pt-barbershop-db
          property: connectionString

databases:
  - name: pt-barbershop-db
    databaseName: pt_barbershop
    user: pt_barbershop
    plan: free
